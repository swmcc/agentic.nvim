{
  "name": "run_claude",
  "description": "Execute Claude Code CLI with a prompt",
  "input_schema": {
    "type": "object",
    "properties": {
      "prompt": {
        "type": "string",
        "description": "The prompt to send to Claude"
      },
      "context": {
        "type": "string",
        "description": "Optional context to include with the prompt"
      },
      "working_directory": {
        "type": "string",
        "description": "Working directory for the command"
      }
    },
    "required": ["prompt"]
  },
  "command": {
    "type": "shell",
    "command": "claude --print \"{{context}}\\n\\n{{prompt}}\""
  }
}
